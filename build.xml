<?xml version="1.0"?>

<project name="MidpSSH" basedir="." default="all">

	<taskdef resource="antenna.properties"/>
	
	<target name="all">
		<antcall target="clean"/>
		<antcall target="make-midp2"/>
		<antcall target="make-midp2-lite"/>
		<antcall target="make-midp1"/>
		<antcall target="make-midp1-lite"/>
	</target>
		
	<target name="make-midp2">
		<antcall target="build">
			<param name="mode" value="midp2"/>
			<param name="obfuscate-normal" value="true"/>
		</antcall>
	</target>
	
	<target name="make-midp2-lite">
		<antcall target="build">
			<param name="mode" value="midp2-lite"/>
			<param name="submode" value="midp2"/>
			<param name="obfuscate-lite" value="true"/>
		</antcall>
	</target>
	
	<target name="make-midp1">
		<antcall target="build">
			<param name="mode" value="midp1"/>
			<param name="obfuscate-normal" value="true"/>
		</antcall>
	</target>
	
	<target name="make-midp1-lite">
		<antcall target="build">
			<param name="mode" value="midp1-lite"/>
			<param name="submode" value="midp1"/>
			<param name="obfuscate-lite" value="true"/>
		</antcall>
	</target>
	
	<target name="make-debug">
		<antcall target="build">
			<param name="mode" value="midp2-debug"/>
			<param name="submode" value="midp2"/>
		</antcall>
	</target>
	
	<target name="run-debug">
    	<antcall target="make-debug">
    		<param name="run" value="true"/>
    	</antcall>
	</target>
	
	<target name="run-midp2">
		<antcall target="make-midp2">
    		<param name="run" value="true"/>
    	</antcall>
	</target>
	
	<target name="run-midp2-lite">
		<antcall target="make-midp2-lite">
    		<param name="run" value="true"/>
    	</antcall>
	</target>

	<target name="init" depends="init-mode,init-submode">
		<property file="build.properties"/>
		<mkdir dir="${dist.dir}"/>
	</target>
	
	<target name="init-mode" if="mode">
		<property file="build-${mode}.properties"/>
	</target>
	
	<target name="init-submode" if="mode">
		<property file="build-${submode}.properties"/>
	</target>
	
	<target name="build" depends="init,package,obfuscate,preverify,build-run">
	</target>
	
	<target name="build-run" if="run">
		<wtkrun jadfile="${dist.dir}/${jad.file}" device="${deviceName}" wait="true"/>
	</target>
		
    <target name="jad">
        <wtkjad jadfile="${dist.dir}/${jad.file}"
                jarfile="${dist.dir}/${jar.file}"
                name="${name}"
                vendor="XK72"
                version="1.1.0">

            <midlet name="${name}" class="app.Main"/>
        </wtkjad>
    </target>

	<target name="clean">
		<property file="build.properties"/>
		<delete dir="${build.dir}"/>
		<delete dir="output"/>
		<delete dir="src-proc"/>
		<delete dir="${dist.dir}"/>
	</target>
	
	<target name="preprocess">
		<delete dir="src-proc"/>
		<wtkpreprocess srcdir="src"
					   destdir="src-proc"
					   symbols="${preprocess.symbols}"
					   verbose="false" />
	</target>
    	
   	<target name="compile" depends="jad,preprocess">
        <mkdir dir="${build.dir}"/>

        <!-- Compile everything, but don't preverify (yet). -->

        <wtkbuild srcdir="src-proc"
                  destdir="${build.dir}"
        		  debug="false"
        	      optimize="true"
                  preverify="false"/>

        <!-- Package everything. Most of the necessary information is
             contained in the JAD file. Also preverify the result this
             time. To obfuscate everything, set the corresponding
             parameter to "true" (requires RetroGuard or ProGuard). The
             version parameter increments the MIDlet-Version by one. -->
	</target>
   		
	<target name="package" depends="compile">
        <wtkpackage jarfile="${dist.dir}/${jar.file}"
                    jadfile="${dist.dir}/${jad.file}"
                    obfuscate="false"
                    preverify="false">

            <!-- Package our newly compiled classes. -->

            <fileset dir="${build.dir}"/>
        	<fileset dir="res"/>
        </wtkpackage>
    </target>
	
	<target name="obfuscate" depends="obfuscate-normal,obfuscate-lite"/>
		
	<target name="obfuscate-normal" if="obfuscate-normal">
    	<!-- Obfuscate -->
    	<wtkobfuscate jarfile="${dist.dir}/${jar.file}"
        			  jadfile="${dist.dir}/${jad.file}"
    				  obfuscator="proguard"
    				  classpath="lib/proguard.jar">
    		<argument value="-keep class ssh.Blowfish" />
    		<argument value="-keep class ssh.DES" />
    		<argument value="-keep class ssh.DES3" />
    		<argument value="-keep class ssh.IDEA" />
			<argument value="-keep class ssh.MD5" />
			<argument value="-keep class ssh.MPN" />
			<argument value="-keep class ssh.NONE" />
    		<argument value="-defaultpackage" />
    		<!--<argument value="-verbose" />-->
		</wtkobfuscate>
    </target>
	
	<target name="obfuscate-lite" if="obfuscate-lite">
		<!-- Obfuscate -->
    	<wtkobfuscate jarfile="${dist.dir}/${jar.file}"
        			  jadfile="${dist.dir}/${jad.file}"
    				  obfuscator="proguard"
    				  classpath="lib/proguard.jar">
    		<argument value="-keep class ssh.Blowfish" />
    		<argument value="-defaultpackage" />
    		<argument value="-allowaccessmodification" />
    		<argument value="-overloadaggressively" />
    		<!--<argument value="-verbose" />-->
		</wtkobfuscate>
	</target>
		
	<target name="preverify">
        <!-- Preverify. -->
        <wtkpreverify jarfile="${dist.dir}/${jar.file}"
                      jadfile="${dist.dir}/${jad.file}"/>
    </target>
	
	<target name="deploy">
		<exec executable="C:\SonyEricsson\J2ME_SDK\OnDeviceDebug\bin\ejava">
			<arg value="install" />
			<arg value="${dist.dir}/${jad.file}" />
		</exec>
	</target>
	
	<target name="source">
		<zip zipfile="${dist.dir}/${zip.file}">
			<fileset dir=".">
				<include name="src/**"/>
				<include name="res/**"/>
				<include name="lib"/>
				<include name="*.txt"/>
				<include name="*.xml"/>
				<include name="*.properties"/>
			</fileset>
		</zip>
	</target>
</project>
